{
  "name": "nook",
  "private": true,
  "scripts": {
    "up": "docker compose up -d",
    "up:build": "docker compose up --build -d",
    "down": "docker compose down",
    "logs": "docker compose logs -f",
    "scrape": "docker compose exec worker node dist/scrape-cli.js",
    "reparse": "docker compose exec worker node dist/reparse-cli.js"
  }
}
